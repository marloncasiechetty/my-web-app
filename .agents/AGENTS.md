# Project Rules

- Always commit and push changes to GitHub (`git add -A && git commit -m "..." && git push`) after completing modifications for each user prompt.
- Automatically apply and commit all code file changes after each prompt.
